if [[ -d "$rvm_path/environments" && -s "$rvm_path/environments/ruby-1.9.2-p290@ancestors" ]] ; then
  \. "$rvm_path/environments/ruby-1.9.2-p290@ancestors"
else
  rvm --create use  "ruby-1.9.2-p290@ancestors"
fi

